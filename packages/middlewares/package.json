{
  "name": "@libresuite/middlewares",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p .",
    "build:watch": "tsc --watch -p .",
    "dev": "pnpm --filter @libresuite/middlewares build:watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@libresuite/logger": "workspace:*",
    "@libresuite/utils": "workspace:*"
  },
  "devDependencies": {
    "@libresuite/constants": "workspace:*",
    "@libresuite/tsconfig": "workspace:*",
    "@libresuite/types": "workspace:*",
    "@types/express": "^5.0.6",
    "@types/module-alias": "^2.0.4",
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3"
  }
}
